# Unique key to identify this project in SonarQube
sonar.projectKey=java-calculator

# Display name shown on the SonarQube UI
sonar.projectName=Java Calculator

# Optional version
sonar.projectVersion=1.0

# Path to source files (relative to this file)
sonar.sources=src

# Path to compiled class files
sonar.java.binaries=target/classes

# Encoding of source files
sonar.sourceEncoding=UTF-8
